StylesPath = .vale/styles
# CI gate fires only on error-level findings — vale-action/reviewdog
# exits non-zero whenever it surfaces anything (the action's
# fail_on_error: false flag does not actually suppress that). Keep
# warning-level rules defined so editors and pre-commit can use them
# for soft feedback, but skip them in CI.
MinAlertLevel = error

Packages = write-good, alex

[*.md]
BasedOnStyles = Vale, BRAINS, write-good, alex

# Vale's built-in spell-check flags BRAINS-specific vocabulary
# (neurodivergent, neurodiversity, AuDHD, etc.). Brand-vocabulary
# enforcement happens via BRAINS.* rules; turn off generic spelling.
Vale.Spelling = NO

# Tone down some write-good rules that conflict with BRAINS voice
write-good.E-Prime = NO
write-good.Passive = warning

# alex catches insensitive language; we want all of it
alex.Ablist = error
alex.Condescending = error
alex.ProfanityLikely = error

# BRAINS rules - all errors by default (configurable per-rule in YAML)
BRAINS.ProtectedPhrases = error
BRAINS.NearParaphrase = error
BRAINS.DoDontTerms = error
BRAINS.DeficitFraming = error
BRAINS.PuzzlePiece = error
BRAINS.BrandCapitalization = error
BRAINS.SentenceLength = warning
BRAINS.PassiveOveruse = warning

[CHANGELOG.md]
BasedOnStyles =
